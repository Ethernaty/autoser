# Повторный аудит 11 пунктов

Проверки выполнены 6 сентября, отчёт завершён 7 сентября 2026. Проверяемый код: `37ef6b9`, ветка `codex/crm-improvements-20260905`, draft PR #5.

Предыдущая общая отметка «все 11 готовы к проверке» не подтверждается. Ни один раздел нельзя считать полностью принятым по исходным требованиям. Реестр заказов наиболее близок к завершению. Ниже `[x]` означает подтверждённую реализацию в указанном объёме, `[ ]` — ошибку, отсутствующую функцию или незавершённую проверку. Этот аудит исправляет отчётность; перечисленные ошибки кода пока не исправлены.

## 1. Дашборд — частично

- [x] Страница открывается, фильтр сотрудников загружается; есть очередь и переход в заказ, график по месяцам.
- [ ] Согласовать период и денежные показатели: выбранные 6 месяцев соседствуют с оплатами за 30 дней и общей выручкой.
- [ ] Считать долг, неназначенные и оплаченные заказы по всей базе: `dashboard-screen.tsx` получает первые 40 заказов и рассчитывает эти показатели из них. Активная очередь также выбирается из этих 40, поэтому более старые активные заказы могут пропасть.
- [ ] В последних изменениях заменить фрагменты UUID вида `Заказ #63d46e` на реальные номера и переходы.
- [ ] Довести отдельные оперативный и аналитический обзоры, проверить крупную базу и все фильтры.

## 2. Реестр заказов — основное реализовано, нужна полная приёмка

- [x] Серверные страницы, поиск, сортировка и итоги реализованы. API-проверка двух страниц с `limit=1` подтвердила одинаковую сводку; поиск вернул соответствующий заказ.
- [x] На странице видны номер, госномер, сумма, оплата, остаток; обзор открывается и ведёт в карточку.
- [x] В коде есть сроки/просрочка, сохранённые фильтры с привязкой к рабочему пространству и экспорт всей выборки постраничными запросами.
- [ ] Дополнить приёмку скачанного CSV, восстановлением сохранённых фильтров, границами дат и большой выборкой. Их наличие в коде не заменяет полного пользовательского теста.

## 3. Приёмка — частично

- [x] В браузере пройдены выбор клиента через поиск, выбор его автомобиля и открытие полей приёмки.
- [x] Через API сохранены пробег, диагностика и фотография; предусмотрены срок, предварительная стоимость и замечания.
- [x] В коде предусмотрены предзаполнение из ссылок и сохранение основной формы черновика.
- [ ] Поиск автомобиля сейчас ограничен первыми 50 автомобилями выбранного клиента. Нет общего поиска автомобиля с автоматическим определением клиента.
- [ ] Черновик не включает фотографии и формы создания нового клиента/автомобиля; ключ не разделён по рабочим пространствам.
- [ ] Согласовать ограничения фотографий: интерфейс допускает пять файлов по 500 КБ, сервер ограничивает суммарные data URL двумя миллионами символов. Полный допустимый интерфейсом набор может быть отклонён.
- [ ] Исправить отображение исполнителей: в форме видны лишние запятые вокруг скобок.

## 4. Карточка заказа — частично

- [x] Проверены переходы к клиенту и автомобилю, обзор состава работ, действий, исполнителей, оплат и истории; сквозные тесты создают позицию и меняют статус.
- [x] Фото приёмки сохраняется; карточка содержит галерею. Есть комментарии и несколько исполнителей.
- [ ] Не реализованы исходно обещанные каталог работ/запчастей, скидки и формальное согласование с клиентом.
- [ ] Довести редактирование данных приёмки и управление вложениями в существующем заказе; полностью проверить историю, которая запрашивается первыми 50 событиями.

## 5. Оплаты и документы — частично, обнаружена ошибка

- [x] Создание оплаты, расчёт остатка и отмена с причиной подтверждены API; причина присутствует в истории.
- [x] HTML-предпросмотр заказ-наряда открывается. Реализованы варианты файлов PDF/HTML/DOCX.
- [ ] Отменённая оплата включается в печатную таблицу как обычная: после отмены тестовых 123 единиц сумма `123,00` осталась в HTML. Контроллер передаёт все платежи, а снимок документа не содержит признак отмены.
- [ ] Документ не получает настройки сервиса: в `WorkOrderDocumentSnapshot` нет логотипа, реквизитов сервиса и валюты. В просмотренном документе стоит общий заголовок AutoService CRM.
- [ ] В интерфейсе денежные суммы выводятся без выбранной валюты.
- [ ] Отмена ошибочного платежа не заменяет учёт возврата клиенту. Отдельные смета и акт не реализованы; выбор PDF/Word/HTML меняет формат одного заказ-наряда.

## 6. Клиенты — частично

- [x] Реестр открывается; серверные сводка, сортировка и фильтры присутствуют.
- [x] Проверены CSV-предпросмотр и импорт: две строки с одним телефоном дали одну готовую строку, один дубль и одну созданную карточку.
- [x] Открываются карточки, есть редактирование и источники клиентов.
- [ ] Объединение двух уже существующих клиентов не реализовано.
- [ ] Дополнительно проверить конфликты при одновременном импорте и крупную базу.

## 7. Карточка клиента — частично, история не работает

- [x] Проверены контакты, ссылки на автомобили и форма редактирования; есть действия создания заказа и автомобиля.
- [ ] История возвращает `422`: экран запрашивает `limit=100`, а API допускает максимум 50 при текущей конфигурации и в `.env.example`.
- [ ] При ошибке истории показывается нулевой долг. В браузере у клиента с заказом на 700 отображались «Текущий долг: 0,00» и `Request validation failed`.
- [ ] Даже при увеличении лимита суммы считались бы только по первым 100 заказам. Нужны полные агрегаты и постраничная история.

## 8. Автомобили — частично, история не работает

- [x] Реестр и текущий владелец открываются; есть быстрый заказ. Сводка считается сервером: даже страница за концом списка сохранила полные показатели.
- [ ] История автомобиля возвращает ту же ошибку `422` из-за `limit=100` при максимуме 50.
- [ ] Сортировка реестра остаётся локальной по текущим 20 строкам.
- [ ] «История владельцев» — список уникальных клиентов из заказов, без дат владения и отдельного учёта перехода собственности.
- [ ] Не реализованы рекомендации ТО; фото есть у заказа, но не полноценная самостоятельная история фото автомобиля. Нужна постраничная история.

## 9. Сотрудники — частично, приоритетные ошибки

- [x] Должность и роль разделены в модели/форме; API возвращает должность и флаг приёма платежей. Изменения данных сотрудника сохраняются.
- [ ] Флаг `can_accept_payments` не участвует в проверке доступа: менеджер с `false` успешно создал платёж, HTTP 200. `RequirePermission` проверяет только роль.
- [ ] Деактивация не работает: интерфейс отправляет только `is_active`, а `EmployeeStatusRequest` требует ещё `can_accept_payments`. Воспроизведён HTTP 422.
- [ ] Приглашения по почте, переназначение открытых заказов и полноценная оценка загрузки не завершены.

## 10. Поддержка — частично

- [x] Страница и фильтры открываются; реализованы номер/подтверждение, статусы и переписка. Через API созданы тестовое обращение и ответ; ответ сохранился.
- [ ] Не реализованы вложения, уведомления и полноценная справка.
- [ ] Обращения существуют внутри рабочего пространства; доставка во внешнюю службу поддержки и понятный процесс ответа оператора не подтверждены.

## 11. Настройки и аккаунт — частично, ошибка сеансов

- [x] Открываются настройки и профиль; логотип сохранился и вернулся в последующем GET. Есть валюта, часовой пояс, язык и тема.
- [x] Смена пароля тестовой учётной записи прошла: старый пароль и старый access token получили 401, новый пароль — 200.
- [ ] Продление сеанса возвращает HTTP 500 как со старым, так и со свежим refresh token. Лог показывает Redis Future, привязанный к другому event loop. Корректный отзыв refresh token таким тестом не подтверждён.
- [ ] Нет восстановления пароля по почте и списка сеансов с отдельным завершением.
- [ ] Рабочие часы представлены текстовой заметкой, полноценного расписания нет.
- [ ] Нужна единая локализация: в боковой панели виден `roles.owner`, в профиле — `owner`; часовой пояс/валюта не применены последовательно ко всем датам и суммам.

## Проверки и границы результата

- Повторно прошли backend smoke, BFF smoke с cookie-сессией, TypeScript typecheck, компиляция Python; Alembic на `20260905_000015`.
- Открыты страницы всех 11 разделов, обзор заказа, HTML-предпросмотр, окно импорта и шаги приёмки. Проверки записей через API выполнялись в отдельных тестовых рабочих пространствах.
- Дополнительно воспроизведены проверки прав на оплату, деактивации, обоих видов истории, отменённого платежа в документе и refresh token; они обнаружили перечисленные ошибки.
- Production-сборка успешно проходила в предыдущем проходе. В этом аудите повторно не запускалась; успешная сборка не подтверждает исправность бизнес-сценариев.
- Не выполнены исчерпывающие проверки всех комбинаций ролей/фильтров, мобильных размеров, большой базы и всех скачиваемых документов. Это не полная приёмка и не разрешение на выпуск.

## Приоритет исправлений

1. Реальное ограничение приёма платежей и рабочая деактивация сотрудников.
2. Загрузка истории клиента/автомобиля и корректный долг без ложного нуля при ошибке.
3. Исключение либо явное обозначение отменённых платежей в документах.
4. Исправление продления сеанса.
5. Полные показатели дашборда и согласованные периоды.
6. Оставшиеся исходные функции из чек-листа, без удаления их из согласованного объёма.

## Сохранность

Код и резервная точка находятся на GitHub. Локальная папка резервной копии существует; восстановление базы в этом аудите не выполнялось. Изменения аудита затрагивают документацию. Основная ветка и production не изменяются.
